<details>
  <summary class="mb-4">Initialization code example</summary>

  <div class="js-example-switcher">
    <div class="flex bg-gray-200 dark:bg-gray-700 px-2 pt-2 pb-4 gap-4">
      <button class="font-bold border-b-4 hover:text-purple-500 hover:border-purple-500">JavaScript</button>
      <button class="font-bold border-b-4 hover:text-purple-500 hover:border-purple-500">TypeScript</button>
    </div>

    <div class="example">
      {% highlight "javascript" %}
import { ViewChannel } from '@uiloos/core';

const config = {
  keepHistoryFor: 100
};

function subscriber(activeList, event) {
  console.log(event);
}

const viewChannel = new ViewChannel(config, subscriber);
{% endhighlight %}
    </div>
    <div class="example">
      {% highlight "typescript" %}
import { ViewChannel ViewChannelEvent } from '@uiloos/core';

const config = {
   keepHistoryFor: 100
};

type FlashMessage = {
  text: string;
  icon: string;
}

function subscriber(
  viewChannel: ViewChannel<FlashMessage, boolean>,
  event: ViewChannelEvent<FlashMessage, boolean>
) {
  console.log(event);
}

const viewChannel = new ViewChannel(config, subscriber);

{% endhighlight %}
  </div>
</details>
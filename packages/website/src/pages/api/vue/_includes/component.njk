{% from "../../_includes/macros.njk" import displayExamples, displayType, displayName, displaySince, displayComment %}

<article>
  {{displayName(def)}}

  <p class="font-mono mb-4 font-extrabold text-red-800">COMPONENT</p>

  {{ displayComment(def) }}

  {{ displaySince(def) }}

  <h2 class="text-2xl">Example</h2>
  {{ displayExamples(def.examples, false) }}
</article>
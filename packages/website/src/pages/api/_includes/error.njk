{% from "./macros.njk" import displayName, displaySince, displayComment %}

{% macro errorPage(def) %}
  <article>
    {{ displayName(def) }}

    <p class="font-mono mb-4 font-extrabold text-red-800">ERROR</p>

    {{ displayComment(def) }}

    {{ displaySince(def) }}

  </article>
{% endmacro %}

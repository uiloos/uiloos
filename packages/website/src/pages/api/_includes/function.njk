{% from "./macros.njk" import displayName, displaySignature %}

{% macro functionPage(def) %}
  <article>
    {{ displayName(def) }}

    <p class="font-mono mb-4 font-extrabold text-green-800">FUNCTION</p>

    {{ displaySignature(def) }}
  </article>

{% endmacro %}
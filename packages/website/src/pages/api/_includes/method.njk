{% from "./macros.njk" import displaySignatures %}

<div class="border px-2 pt-4 mb-4">
  <h3 class="h3 mb-2 font-mono" id="{{def.name}}"><a href="#{{item.name}}">{{item.name}}</a></h3>

  {{ displaySignatures(item.signatures) }}
</div>